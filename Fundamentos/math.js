const raio = 5.6 //declarando uma constante
const area = Math.PI * Math.pow(raio, 2)//objeto Math, tem como constante o PI, e pow(função de Math) exige 2 parâmetro, o raio e a exponenciação

console.log(area)