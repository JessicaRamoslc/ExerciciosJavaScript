const[a, b, c, d] = [3, 5, 1, 15]//operador destructuring

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b; 
const divisao = d/a; 
const modulo = a % 2; 

console.log(soma, subtracao, multiplicacao, divisao, modulo)

//operador binário operam com 2 operandos, ex: a + b
//operador unário opera com 1 operando, ex -a(lê-se menos a)