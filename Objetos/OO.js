// CÓDIGO NÃO EXECUTÁVEL!

// Linguagem Procedural: linguagem manipulando dados globais ou internos e retorna um valor

processamento(valor1, valor2, valor3) //valores processados por uma função

//OO
objeto = {
    valor1, //valores, ou seja, atributos internos do objeto
    valor2, 
    valor3,
    processamento(){//comportamentos

    }
}

//voce passa a invocar esses processamentos a partir do dado
objeto.processamento()//função pertence ao objeto, o foco passou a ser o objeto.

//o objeto funciona como essa cápsula que engloba comportamento e atributos

// Principio do encapsulamento: ter os detalhes de implementação escondidos e mostrar pra quem precisa usar aquele sistema,através de interface 

//Principio da Herança. Atributos que vem de Pai e Mãe. JS é herança de protótipo. Conceito do mais pro menos genérico. relação 'é um'. Entretanto, Priorize a composição ao invés da herança. 

// Polimorfismo: Multiplas formas de utilização. 